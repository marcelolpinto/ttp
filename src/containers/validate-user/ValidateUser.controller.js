import { BaseController } from '../../helpers';
import { toast } from 'react-toastify';

export class ValidateUserController extends BaseController {
  constructor({ toState, getProps, getState }) {
    super({ toState, getProps, getState });
  }
}